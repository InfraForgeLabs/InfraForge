apiVersion: v1
kind: Service
metadata:
  name: {{APP_NAME}}-svc
  namespace: {{NAMESPACE}}
  labels:
    app: {{APP_NAME}}
spec:
  type: ClusterIP
  selector:
    app: {{APP_NAME}}
  ports:
    - protocol: TCP
      port: {{PORT}}
      targetPort: {{PORT}}
