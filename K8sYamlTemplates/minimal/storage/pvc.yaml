apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{APP_NAME}}-pvc
  namespace: {{NAMESPACE}}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{STORAGE_SIZE}}
  storageClassName: standard
