# ------------------------------------------------------------
# 🧱 Docker-Compose Networks
# ------------------------------------------------------------
networks:
  {{APP_NAME}}_net:
    driver: bridge
