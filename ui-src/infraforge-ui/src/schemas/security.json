{
  "id": "security",
  "name": "Security",
  "description": "Generate security and DevSecOps templates",
  "fields": [
    {
      "key": "scanner",
      "label": "Security Tool",
      "type": "select",
      "required": true,
      "options": ["trivy", "zap", "snyk"]
    },
    {
      "key": "severity",
      "label": "Minimum Severity",
      "type": "select",
      "required": false,
      "options": ["low", "medium", "high", "critical"]
    }
  ]
}
