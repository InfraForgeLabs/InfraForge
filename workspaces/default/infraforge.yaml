workspace: default

output:
  base_dir: ./output
  overwrite: false

generator:
  log_level: info
  keep_temp: false

diff:
  enabled: true
  ignore_whitespace: true

security:
  allow_shell: false
  allow_network: false
