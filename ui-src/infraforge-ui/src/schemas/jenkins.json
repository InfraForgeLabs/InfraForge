{
  "id": "jenkins",
  "name": "Jenkins",
  "description": "Generate Jenkins pipelines and job templates",
  "fields": [
    {
      "key": "pipeline_type",
      "label": "Pipeline Type",
      "type": "select",
      "required": true,
      "options": ["ci", "cd", "ci-cd"]
    },
    {
      "key": "language",
      "label": "Project Language",
      "type": "select",
      "required": true,
      "options": ["node", "python", "java", "go"]
    }
  ]
}
